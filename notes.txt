When we want to create click events, we create a function inside the component function before the return statement. In this case we have created handleClick function inside the Home component (See Home.jsx ln 3). Then we invoke the function on an element without the parentheses (See Home.jsx ln 15). 