To view the project in a browser, go to the project directory and run the following command:

    `npm start`

This will start a local development server on port 3000.

What is being shown in the browser is Script.jsx which is App component.