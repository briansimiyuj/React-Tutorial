We can use JavaScript inside templates by surrounding it with {} as shown in Home.jsx ln20.

Key property helps React to keep track of each item in the DOM as it outputs it. It's like an ID.

Video #10