Let's now create a 404 page (NotFound.jsx). Then we create a new Route for it in the root component with a path of * (Script.jsx ln48). This means if the Route is not any of our component, it should direct the user to NotFound component.

Video #32