In JSX, we can output dynamic values in it like shown in Script.jsx. The only types we can't output are booleans and objects.

Objects properties can however be outputted.

JSX converts any data type to a string before outputting it as a dynamic value