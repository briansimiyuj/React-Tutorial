Let's say for one reason or another we don't have the node_modules directory, our React project will not work since it depends on the node_modules directory.

So to install node_modules directory, run the following command:

    `npm install`

This will go through package.json and install all the dependencies that are listed there.

Video #3